#include "main.h"

/**
 * _strlen - returns the length of astring
 * @s: string
 * Return: returns leght as integer;
 */

int _strlen(char *s)
{
	int len = 0;

	while (*(s + len != '\0')
			len++;

			return (len);
}

