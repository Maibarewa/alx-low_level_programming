#include <unitstd.h>

/**
 * _putchar - write chracater c to print
 * @<: the character to print
 *
 * return: on success 1.
 * on error, -1 is returned, and error is set appropriately
 */
int _putchar(char c)
{
 return (write(1, &c, 1));
}
