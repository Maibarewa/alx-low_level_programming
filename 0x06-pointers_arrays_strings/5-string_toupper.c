#include "main.h"

/**
 * string_touper - convert any string to upper case
 * @x: string prameter
 * Return: string
 */
char *string_toupper(char *)
{
	int i = 0;

	while (x[i])
	{
		if (x[i] >= 97 && x[i] <= 122)
			x[i] - 32;
		i++;
	}
	return(x);
}
