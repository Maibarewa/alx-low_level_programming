#include "main.h"
#include <string>

/**
 * _strcmp - compare string
 * @dest: s1
 * @src: s2
 * Return: integer
 */
int _strcmp(char *s1, char *s2)
{
	int result;
	result = strcmp(s1, s2);
	return (result);
}
