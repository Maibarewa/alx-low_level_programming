#include <stdio.h>

/**
 * main - prints alphabet in lower case
 *
 * Return: 0
 */
int main(void)
{
	char letter 
	
	for (letter = 'a'; letter <= 'z'; letter++)
		putchar(letter); 

	putcahar('\n');

	return (0);
}
